<!DOCTYPE html>
<html lang="en">

  <head>

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>MyZaGa</title>

    <!-- Bootstrap core CSS -->
    <link href="/stylesheets/bootstrap.min.css" rel="stylesheet">

    <!-- Custom styles for this template -->
    <link href="/stylesheets//scrolling-nav.css" rel="stylesheet">

  </head>

  <body id="page-top">

    {{>nav-bar}}

    <header class="bg-blue text-white">
      <div class="container text-center">
        <h1>Users</h1>
      </div>
    </header>

      <section style="user-list">
      <div class="container text-center">
        <div class="row">
        <div class="col-lg-12 mx-auto">
        {{#users.length}}
            <table class="table table-hover bg-light rounded-1">
            <tr>
            <th>Login</th>
            <th>Fullname</th>
            <th>Registered</th>
          </tr>
          {{#users}}
          <tr>
            <td><a href="/users/{{id}}"><img class="rounded-circle" style="position: relative; margin:5px; height:30px; width:30px;" src="{{avaUrl}}"> {{login}}</a><span style="margin-left: 5px; padding:2px;" class="user-status online text-white rounded-circle">online</span></td>
            <td>{{fullname}}</td>
            <td>{{registeredAt}}</td>
          </tr>
          {{/users}}
            </table>
        {{/users.length}}

        {{^users.length}}
           No users were found.
        {{/users.length}}
        <a class ="btn btn-primary float-right" href="/users/new">Add new user</a>
        </div>
        </div>
      </div>
    </section>

    {{>footer}}

  </body>

</html>
